# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
   name: test

steps:
- task: PowerShell@2
  inputs:
    targetType: 'inline'
    script: |
      cd C:\Users\Administrator\Downloads\OpenText_SAST_Fortify_Samples_25.4.0\advanced\javaWebApp
      sourceanalyzer .b test1 *.*
      sourceanalyzer -b test1 -scan -f C:\azuretest.fpr